/*TO DO
needs a params. when clicking the link on homepage, needs to get the id in URL
THUNK: take params, findbypk, fetch the right stories per space and put them in store
Selector fetches and gives to details page
detailspage shows the stories as well
*/

//tool imports

//component and function imports

//function

export default function Details() {
  return <div>Hi from detailspage</div>;
}
